<odoo>
    <record id="view_product_tree_inherit_stock_history" model="ir.ui.view">
        <field name="name">product.product.tree.inherit.stock.history.location</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="stock.view_stock_product_tree"/>
        <field name="arch" type="xml">
            <!-- Añadir el campo location_id al final de la vista -->
            <xpath expr="//tree" position="inside">
                <field name="location_id" string="Location"/>
            </xpath>
        </field>
    </record>
</odoo>
